name: Verdant Crown Compact Phone Test APK

on:
  push:
    branches: [phytosense-verdant-final]
    paths:
      - '.github/workflows/verdant-test-apk.yml'
      - 'lib/**'
      - 'assets/branding/**'
      - 'android/app/src/main/res/**'
      - 'pubspec.yaml'
      - 'test/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-test-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout validated Verdant source
        uses: actions/checkout@v4
        with:
          ref: phytosense-verdant-final

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve dependencies
        run: flutter pub get

      - name: Format sources
        run: dart format lib test

      - name: Analyze additive animation changes
        run: flutter analyze --no-fatal-infos

      - name: Run regression tests
        run: flutter test

      - name: Build compact ARM64 phone-test APK
        run: |
          set -euo pipefail
          flutter build apk --profile --split-per-abi
          test -s build/app/outputs/flutter-apk/app-arm64-v8a-profile.apk
          cp build/app/outputs/flutter-apk/app-arm64-v8a-profile.apk PhytoSense_AI_VERDANT_CROWN_STARTUP_FIXED_ARM64.apk

      - name: Verify package identity
        shell: bash
        run: |
          set -euo pipefail
          sdk_root="${ANDROID_SDK_ROOT:-${ANDROID_HOME}}"
          app_id="$("${sdk_root}/cmdline-tools/latest/bin/apkanalyzer" manifest application-id PhytoSense_AI_VERDANT_CROWN_STARTUP_FIXED_ARM64.apk)"
          test "${app_id}" = "com.harjeet.phytosense"

      - name: Upload compact phone-test APK
        uses: actions/upload-artifact@v4
        with:
          name: PhytoSense-AI-VERDANT-CROWN-STARTUP-FIXED-ARM64
          path: PhytoSense_AI_VERDANT_CROWN_STARTUP_FIXED_ARM64.apk
          if-no-files-found: error
          retention-days: 14
